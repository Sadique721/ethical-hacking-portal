# Django Settings Configuration
SECRET_KEY=django-insecure-s&zn1go+$19c=(waf8dz24dcv9civhwk-)jy8qzq*!(i$id2k*
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,*

# Database Configuration (Defaults to SQLite if not provided)
# For PostgreSQL: postgres://user:password@host:port/database_name
# DATABASE_URL=postgres://hacker:password@localhost:5432/hacking_portal

# Caching Configuration (Defaults to LocMemCache if not provided)
# REDIS_URL=redis://localhost:6379/1

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
